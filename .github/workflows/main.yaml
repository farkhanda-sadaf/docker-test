name: Build and Publish Docker image

on:
  push:
    branches:
      - main
permissions:
    id-token: write   
    contents: read  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
            role-to-assume: arn:aws:iam::327510741480:role/Github-role
            role-session-name: samplerolesession
            aws-region: us-east-1 

      - name: Push image from Amazon ECR
        run: |
            aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 327510741480.dkr.ecr.us-east-1.amazonaws.com
            docker build -t 327510741480.dkr.ecr.us-east-1.amazonaws.com/fanda-repo:latest .
            docker push 327510741480.dkr.ecr.us-east-1.amazonaws.com/fanda-repo:latest
            aws ecs run-task --cluster fandas-cluster --launch-type FARGATE --task-definition fandas-task --network-configuration 'awsvpcConfiguration={subnets=[subnet-094ee6ba7486e1a62],securityGroups=[sg-0feb91a1971b3555b (fanda-sg)]}'


    
